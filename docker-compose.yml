services:
  api:
    build: .
    ports: ["8000:8000"]
    volumes: ["./backend:/app"]
  web:
    image: node:20
    working_dir: /app
    command: sh -c "npm install && npm run start"
    ports: ["3000:3000"]
    volumes: ["./frontend:/app"]
    environment:
      - VITE_API=http://localhost:8000
